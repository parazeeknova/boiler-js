# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#

pre-commit:
  jobs:
    - name: lint frontend
      run: cd frontend && npm run lint

    - name: lint backend
      run: cd backend && npm run lint

    - name: lint global
      run: npm run lint
